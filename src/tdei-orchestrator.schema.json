{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Schema for tdei orchestrator config",
    "type": "object",
    "properties": {
        "$schema": {
            "type": "string"
        },
        "workflows": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "type": {
                        "type": "string",
                        "anyOf": [
                            {
                                "const": "TRIGGER",
                                "title": "Defines that the worklow can be triggered via orchestrator"
                            },
                            {
                                "const": "HANDLER",
                                "title": "Defines that the worklow can handle the response messages and cannot be triggered via orchestrator"
                            }
                        ]
                    },
                    "stage": {
                        "type": "string",
                        "description": "Defines the workflow stage"
                    },
                    "group": {
                        "type": "string",
                        "description": "Defines the workflow group"
                    },
                    "identifier": {
                        "type": "string",
                        "description": "Uniquely identifies the workflow."
                    },
                    "generic_workflow": {
                        "type": "boolean",
                        "description": "If 'true', confirms workflow to execute generic workflow, which forwards the message to the next step without any processing and not required to define Workflow request/response class"
                    },
                    "next_steps": {
                        "type": "array",
                        "description": "Defines the handlers to respond to the workflow request",
                        "items": {
                            "type": "object",
                            "properties": {
                                "process_identifier": {
                                    "type": "string",
                                    "description": "Name of the process handler"
                                },
                                "params": {
                                    "type": "object",
                                    "properties": {
                                        "response_message_identifier": {
                                            "type": "string",
                                            "description": "Workflow identifier"
                                        },
                                        "topic": {
                                            "type": "string",
                                            "description": "Topic to publish the message"
                                        }
                                    },
                                    "required": []
                                },
                                "delegate_worflow": {
                                    "type": "array"
                                }
                            },
                            "required": [
                                "process_identifier",
                                "params"
                            ]
                        }
                    }
                },
                "required": [
                    "type",
                    "stage",
                    "group",
                    "identifier"
                ]
            }
        },
        "subscriptions": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "description": {
                        "type": "string",
                        "description": "Free text description"
                    },
                    "topic": {
                        "type": "string",
                        "description": "Topic name where the message are posted"
                    },
                    "subscription": {
                        "type": "string",
                        "description": "Subscription name to listen to topic messages"
                    }
                },
                "required": [
                    "description",
                    "topic",
                    "subscription"
                ]
            }
        }
    },
    "required": [
        "workflows",
        "subscriptions"
    ]
}